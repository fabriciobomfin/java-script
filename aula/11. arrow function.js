// Criar uma arow function.
const somar = (a, b) => {
    return a + b
}

const subtrair = (a , b) => a - b


// Chamando a função
const soma = soma(2,3)
const subtacao = subtrair(2,3)

// Exibir.
console.log(`Soma: ${soma}`)
console.log(`Subtração: ${soma}`)